class Solution {
    public int removeElement(int[] nums, int val) {
        int l = 0;
        for (int a = 0; a < nums.length; a++)
        {
            if (nums[a] != val)
            {
                nums[l] = nums[a];
                l++;
            }
        }
        return l;
    }
}
