class Solution {
    public int lengthOfLastWord(String s) {
        String[] temp = s.split(" ");
        if (temp.length == 0)
        return 0;
        else
        return temp[temp.length - 1].length();
    }
}
